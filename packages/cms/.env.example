VITE_API_BASE_URL=http://localhost:3000

# Mobile app preview iframe target (Expo web). Default points at local
# `expo start --web` on port 8081. Override per-environment for staging
# preview (e.g. https://staging-preview.a-idol.dev).
VITE_MOBILE_PREVIEW_URL=http://localhost:8081/m
