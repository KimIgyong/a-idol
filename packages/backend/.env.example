# Backend-specific overrides. Root .env is usually enough for local dev.
# This file exists so Prisma CLI can resolve DATABASE_URL from packages/backend.
DATABASE_URL=postgresql://aidol:aidol_dev@localhost:5433/aidol?schema=public
